<div>
    <h2>Instructions</h2>
    <ol>
        <% if (instructions_titles.length > 0) {for (int i = 0; i < instruction_titles.length; i++) { %>
            <li><b><%= instruction_titles[i] %></b><%= instructions_details[i] %></li>
        <% }} %>
    </ol>
</div>